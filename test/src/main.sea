# Prints the first n fibonacci numbers
fun fib(int n):
    int i = 0
    int x, y = 0, 1

    while i < n:
        # Prints Enumeration + FibNum * i
        cprint(1 + i + y * 1i)

        x, y = y, x + y
        i += 1

fun main() -> int:
    fib(10)
    return 0
